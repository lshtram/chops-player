# chops-player — Agent Patterns

> This file accumulates patterns discovered during development sessions.
> When an agent encounters friction, a workaround, or a non-obvious solution, it adds an entry here.
> Read the YAML front matter before starting any non-trivial task.

---
patterns:
  - id: []
    description: "No patterns recorded yet. First entry will be added after Phase I."
---

## How to Add a Pattern

Each entry should have:
- **ID:** `CP-NNN` (chops-player pattern number)
- **Context:** What situation triggers this pattern
- **Problem:** What goes wrong without it
- **Solution:** The exact approach that works
- **Evidence:** Commit SHA or test name where this was validated

---

_Patterns will be added here as the project evolves._
