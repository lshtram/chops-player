// Vitest global test setup
// Add any global mocks or polyfills here

import "@testing-library/jest-dom";

// Web Audio API mock (not available in jsdom)
// Individual tests that need it should mock at the test level
